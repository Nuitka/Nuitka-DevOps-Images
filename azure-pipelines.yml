trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Nuitka(631b7fbe-7e89-4f3b-a230-3820ab93e1ac)'
    scriptType: 'bash'
    scriptLocation: 'scriptPath'
    scriptPath: 'build-windows.sh'
    addSpnToEnvironment: true